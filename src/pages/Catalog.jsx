import { Box } from '@mui/material';
import CatalogPage from '../components/Catalog'

const Catalog = () => { 
    return (
        <Box sx={{height: 'auto', mt: 6}}>
             <CatalogPage />
        </Box>
    )
}

export default Catalog;